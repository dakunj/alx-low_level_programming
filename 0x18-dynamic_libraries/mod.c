#include <stdio.h>
#include <stdlib.h>

/**
 * mod - the remainder after an int is divided by another int
 * @a: first int
 * @b: second int
 * return: modulus
 */
int mod(int a, int b)
{
	return(a % b);
}
